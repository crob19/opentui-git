type BranchDetail {
  name: String!
  commit: String!
  label: String!
  current: Boolean!
  ahead: Int!
  behind: Int!
}

type Branches {
  current: String!
  all: [String!]!
  branches: [BranchDetail!]!
}

type BranchResult {
  success: Boolean!
  branch: String
}

type MergeOutcome {
  success: Boolean!
  merges: [String!]!
  conflicts: [String!]!
  result: String!
}

extend type Query {
  branches: Branches!
  defaultBranch: String!
}

extend type Mutation {
  createBranch(name: String!, source: String): BranchResult!
  checkoutBranch(name: String!): BranchResult!
  renameBranch(oldName: String!, newName: String!): BranchResult!
  deleteBranch(name: String!, force: Boolean): BranchResult!
  mergeBranch(name: String!): MergeOutcome!
}
